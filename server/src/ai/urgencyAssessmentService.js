export function assessUrgency({redFlags,severity,symptoms}){if(redFlags.length)return'EMERGENCY';if(severity==='SEVERE'||symptoms.includes('difficulty_breathing')||symptoms.includes('chest_pain'))return'HIGH';if(severity==='MODERATE'||symptoms.some(x=>['fever','vomiting','diarrhoea','painful_urination'].includes(x)))return'MODERATE';return severity==='MILD'?'LOW':'ROUTINE';}export function actionFor(urgency,department){if(urgency==='EMERGENCY')return'Seek urgent medical attention now. Go to the nearest emergency department or contact local emergency services.';if(urgency==='HIGH')return'Seek same-day medical assessment.';if(urgency==='MODERATE')return'Arrange a medical review soon.';return`Book a routine appointment${department?` with ${department.name}`:''}.`;}
