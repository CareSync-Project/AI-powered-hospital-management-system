import { useEffect, useState } from 'react';
import { patientService } from '../../services/patientService';
import PatientCardsPage from './PatientCardsPage';
export default function PatientProfilePage(){const[form,setForm]=useState(null),[message,setMessage]=useState('');useEffect(()=>{patientService.profile().then(r=>setForm(r.data)).catch(e=>setMessage(e.message))},[]);if(!form)return <section className="patient-panel"><p>{message||'Loading profile…'}</p></section>;const save=async event=>{event.preventDefault();try{const required=['phone','address','city','region'],payload=Object.fromEntries(required.map(key=>[key,form[key]]));payload.emergencyContactName=form.emergencyContactName||null;payload.emergencyContactPhone=form.emergencyContactPhone||null;const response=await patientService.updateProfile(payload);setForm(response.data);setMessage('Profile updated.')}catch(error){setMessage(error.message)}};return <><section className="patient-panel"><h2>Profile</h2>{message&&<p role="status">{message}</p>}<form className="patient-form" onSubmit={save}><div className="profile-name"><strong>{form.firstName} {form.lastName}</strong><span>{form.user?.email}</span></div>{['phone','address','city','region','emergencyContactName','emergencyContactPhone'].map(key=><label key={key}>{key.replace(/([A-Z])/g,' $1')}<input required={['phone','address','city','region'].includes(key)} value={form[key]||''} onChange={event=>setForm({...form,[key]:event.target.value})}/></label>)}<button>Save profile</button></form></section><PatientCardsPage/></>}
